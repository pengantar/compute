---
label: News
hide_body: false
is_partial: false
fields:
- type: text
  name: title
  label: title
  description: Title of your news article
  config:
    required: true
- type: field_group
  name: image
  label: image
  description: add a featured image
  fields:
  - type: text
    name: name
    label: name
    description: what is the name of the featured image? (no extension)
    config:
      required: true
  - type: text
    label: extension
    name: type
    config:
      required: true
    description: ".eg jpg, svg, png, webp ... (in small caps)"
  - type: text
    name: description
    label: description
    description: Add a description for the featured image. (for good SEO)
    config:
      required: true
